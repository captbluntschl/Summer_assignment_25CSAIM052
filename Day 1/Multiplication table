#include <stdio.h>
int main() 
{
    int i,num;
    printf("Enter a number");
    scanf("%d",&num);
    
    printf("\n multiplication table of %d\n",num);
    
    for(i=1;i<=10;i++)
    {
        printf("%dX%d=%d\t\n",num,i,num*i);
    }
    return 0;
}