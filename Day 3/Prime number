#include <stdio.h>
int main()
{ 
    int num,i,count=0;
    printf("Enter the number");
    scanf("%d",&num);
    for(i=1;i<=num;i++)
    {
        if(num%i==0)
        { count++;
        }
    }
    if(count==2)
    {printf("the number is prime number %d",num);
    }
    else
    {printf(" the number is not prime %d",num);
    }

    return 0;
}