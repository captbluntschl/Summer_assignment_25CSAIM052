#include <stdio.h>
int main()
{ 
    int num,rem,product=1;
    printf("Enter the number");
    scanf("%d",&num);
    while(num>0)
    {
        rem=num%10;
        product=product*rem;
        num=num/10;
    }
    printf("The product is %d:",product);
    return 0;
}
